# Simple tools to work with point cloud

- `curb_aggregate` requires a point cloud topic. It combines a few clouds together (using `tf` lookup) which makes the cloud denser and hence easier to process
